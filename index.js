let name = "Fernando";
let age = 23;

console.log(name, age);

const showUserInfo = (userName, userAge) =>
  `The username is ${userName}, the user is ${userAge} years old`;
console.log(showUserInfo("Fernando", 20));
